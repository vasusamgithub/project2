apiVersion: v1
kind: Service
metadata:
  name: {{ include "vasu.fullname" . }}
  labels:
    {{- include "vasu.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "vasu.selectorLabels" . | nindent 4 }}
