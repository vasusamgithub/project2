apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vasu.fullname" . }}-test-connection"
  labels:
    {{- include "vasu.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vasu.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
